import './bootstrap';

// Import komponen Tab dari library "tw-elements"
import { Tab, initTE } from 'tw-elements';

// Inisialisasi komponen Tab
initTE({ Tab });


// Inisialisasi Carousel
initTE({ Carousel });

